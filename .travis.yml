language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-bc-generator' ]; then cd .. && eval "mv $currentfolder generator-bc-generator" && cd generator-bc-generator; fi

